<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="jdbc.properties"/>
    <typeAliases>
        <!--<package name="com.xxx.domain"/>-->
        <package name="com.gmu.crm.settings.domain"/>
    </typeAliases>
<!--    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor"/>
    </plugins>-->
    <environments default="myDatabases">
        <environment id="myDatabases">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.user}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--<mapper resource="com/gmu/crm/settings/dao/UserDao.xml"/>-->
        <package name="com.gmu.crm.settings.dao"/>
    </mappers>
</configuration>